import { Button } from "@mui/material";
import { Link } from "react-router-dom";

import { Card } from "ui/Card";

import * as S from "./styles";

export const PaymentInfo = ({ info }) => {
  console.log(info);
  return <S.Wrapper>fgh</S.Wrapper>;
};
